# Cyclismo Bootstrap Web Console

Local web UI for viewing `Cyclismo/bootstrap_database.json` and regenerating the bootstrap data.

## Run

```bash
cd /Users/carambula/Documents/Cyclismo/bootstrap_web
node server.js
```

Open `http://localhost:4188`.

Theme preset admin is available at `http://localhost:4188/themes`.


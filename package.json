{
  "name": "min-apps",
  "version": "0.0.0",
  "private": true,
  "description": "Monorepo for the min apps suite: WatchedIt, podlink, yourtube, Cyclismo guide",
  "author": "carambula",
  "license": "MIT",
  "packageManager": "npm@11.9.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "studio": "npm run dev --workspace=@min-apps/design-studio",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}

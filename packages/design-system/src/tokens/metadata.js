/**
 * Inline metadata in list subtitles, headers, captions, and detail rows.
 * Separate segments with exactly three ASCII spaces — never middle dots (·),
 * bullet dots (•), pipes, or slashes as separators.
 */
export const metadataSeparator = '   ';
